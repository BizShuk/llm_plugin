# Define database sources
[[sources]]
id = "local"
dsn = "sqlite:///Users/shuk/projects/crawler/inf/db/local.sqlite"
connection_timeout = 60

# Configure tools for each source
[[tools]]
name = "execute_sql"
source = "local"
readonly = false
max_rows = 1000
